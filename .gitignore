# Next.js
/.next/
/out/

# dependencies
/node_modules

# production
/build

# env
.env
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
next-env.d.ts
*.tsbuildinfo

# sanity
.sanity

# carpeta de fotos crudas usada solo una vez para el importador masivo
# (bin/import-a-sanity.mjs) - las fotos ya viven en Sanity, no las usa el
# sitio en runtime. ~86MB, se deja fuera del repo para no inflarlo.
/productos/

# estado local de Claude Code, no es parte del proyecto
/.claude/
